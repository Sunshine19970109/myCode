json(javascript object notation) 声明
(1)json是什么?
	是一种轻量级的数据交换格式。
	注:json借鉴了javascript的部分语法
	XML解析速度慢，文档大
	注:
	数据交换:是指将要转换的数据转换成一种与平台
无关的数据格式(比如xml),然后发送给接收方来处理。
	
JSON是一种轻量级的数据交换格式

编写程序，使用JSON实现三级联动的下拉列表
	1 页面--下拉框，解析返回的JSON对象--参考，API
	2 监测前一个的值返回后面下拉选的值--异步请求--copy+参考
	3 怎么才能和JSON有关呢。
	只要与对象的数据有关就可以与JSON有关，就可以用JSON来
	传数据。

ajax中服务器输出的值，和浏览器接收的值的个数必须一致，否则













