1.练习OverrideDemo
2.练习OverrideOverloadDemo
3.练习StaticDemo

作业答案:
1.我只能在地上奔跑
2.SlowPoint move parameter
Point move
3.D.同时使用不同包中相同类名的类，包名不能省略---------正确
在Java语言中，用包（package）的概念来解决命名冲突的问题。但是，如果不考虑到命名冲突的问题，则可以不用指定包名，虽然这并不是一个好习惯。
4.CD
5.BD
6.D
7.ACD
C. final 关键字修饰的方法不可以被重写--t
A.final关键字如果用于修饰成员变量，那么该成员变量必须在声明时初始化--f
D.final 关键字如果用于修饰方法，该方法所在的类不能被继承--f
8.D
B. public static final String FOO = "foo";
D.public final static String FOO = "foo";
都正确

instanceof 是 Java 的一个二元操作符，
它的作用是测试它左边的对象是否是它右边的类的实例，返 回 boolean 类型的数据。Teahcer 和 Student 之间没有继承关系不能做强制类型转换。

如果在源文件中声明了一个public接口，则其名称必须和源文件名一致。-----正确

final 修饰方法，在子类中不能被重写。
静态初始化器可以有多个

栈是一种存取受限的线性结构

静态方法中不能引用其外部的非静态成员
匿名内部类中已覆盖 了hashCode 方法。

abstract可以修饰方法和类，不能修饰属性。抽象方法没有方法体，即没有
大括号{}
类可以继承接口的意思

对于顶层类(外部类)来说，只有两种修饰符：public和默认(default)。
因为外部类的上一单元是包，所以外部类只有两个作用域：同包，任何位置。
因此，只需要两种控制权限：包控制权限和公开访问权限，也就对应两种控制修饰符：public和默认(default)。

方法重写返回值类型如果是基本类型应与父类的一致

抽象方法不可以是静态的，不可以有方法体，不可以在普通类中定义，可以在接口中和抽象类中定义。
重写要求方法签名完全相同，返回值类型如果是基本类型或无返回值时必须一致。重载要求方法名相同且参数列表必须不同，与返回值类型无关。
局部变量在使用前可以先不被初始化
栈（stack）在计算机科学中是限定仅在表尾进行插入或删除操作的线性表。

重载：方法名相同且参数列表不同，与返回值类型无关。
抽象方法不可以是静态的，不可以有方法体，不可以在普通类中定义，可以在接口中和抽象类中定义。











































